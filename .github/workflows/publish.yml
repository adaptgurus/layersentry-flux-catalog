name: HISTORICAL - CloudStack qualification publication (disabled)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  historical-cloudstack-publication-disabled:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo "Historical CloudStack qualification workflow is disabled."
          echo "Use catalog/v1/catalog.json and validate-catalog.yml for current LayerSentry RKE2 catalog work."
