name: Validate LayerSentry data services
on:
  pull_request:
    paths:
      - 'apps/data-services/**'
      - 'clusters/e1/**'
      - 'release/**'
      - 'scripts/verify-data-services.sh'
      - '.github/workflows/validate-data-services.yml'
  push:
    branches:
      - main
    paths:
      - 'apps/data-services/**'
      - 'clusters/e1/**'
      - 'release/**'
      - 'scripts/verify-data-services.sh'
      - '.github/workflows/validate-data-services.yml'
permissions:
  contents: read
jobs:
  validate:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          persist-credentials: false
      - name: Install upstream-qualified Helm toolchain
        uses: azure/setup-helm@1a275c3b69536ee54be43f2070a358922e12c8d4
        with:
          version: v3.14.0
      - name: Verify exact OpenEverest and Flux contract
        shell: bash
        run: bash scripts/verify-data-services.sh
